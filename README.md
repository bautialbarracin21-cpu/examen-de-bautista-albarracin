# Este es el examen de Nombre Apellido

## Comandos utilizados:

git config --global user.name
git config --global user.email
git clone
cd
code .
git checkout -b
git add .
git commit -m
git push
git checkout main
git merge
